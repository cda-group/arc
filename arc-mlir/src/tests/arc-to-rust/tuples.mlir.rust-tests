#[cfg(test)]
mod tests {
  #[test]
  fn test() {
    let tmp = crate::makemeatuple();
    let tmp1 = crate::makemeatuple();

    assert_eq!(tmp.0, 7);
    assert_eq!(tmp.1, 17);

    assert_eq!(crate::bool_tuple(),false);
    assert_eq!(crate::const_tuple(),17);
    assert_eq!(crate::tuple_access(tmp),17);
    assert_eq!((crate::make_nested(tmp1).2).1,17);
  }
}
