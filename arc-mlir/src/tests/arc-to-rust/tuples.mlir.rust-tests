#[cfg(test)]
mod tests {
  #[test]
  fn test() {
    let tmp = crate::makemeatuple();

    assert_eq!(tmp.0, 7);
    assert_eq!(tmp.1, 17);

    assert_eq!(crate::bool_tuple(),false);
    assert_eq!(crate::const_tuple(),17);
    assert_eq!(crate::nonconst_tuple(tmp),17);
  }
}
