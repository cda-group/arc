#[cfg(test)]
mod tests {
 use crate::arctorusttuples::*;
  #[test]
  fn test() {
   let tmp = makemeatuple();
   let tmp1 = makemeatuple();
   let tmp = makemeatuple();
   let tmp1 = makemeatuple();
   assert_eq!(tmp.0, 7);
   assert_eq!(tmp.1, 17);

   assert_eq!(bool_tuple(),false);
   assert_eq!(const_tuple(),17);
   assert_eq!(tuple_access(tmp),17);
   assert_eq!((make_nested(tmp1).2).1,17);
   assert_eq!(bool_tuple(),false);
   assert_eq!(const_tuple(),17);
   assert_eq!(tuple_access(tmp),17);
   assert_eq!((make_nested(tmp1).2).1,17);
  }
}
