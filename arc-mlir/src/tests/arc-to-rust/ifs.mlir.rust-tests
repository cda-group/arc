#[cfg(test)]
mod tests {
  #[test]
  fn test() {
    assert_eq!(crate::test_0(), 65);
    assert_eq!(crate::test_1(true, 0, 1), 0);
    assert_eq!(crate::test_1(false, 0, 1), 1);
    assert_eq!(crate::test_1(true, 1, 0), 1);
    assert_eq!(crate::test_1(false, 1, 0), 0);
  }
}
