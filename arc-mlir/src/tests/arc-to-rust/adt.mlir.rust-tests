#[cfg(test)]
mod tests {
    use crate::arctorustadt::*;

    #[data]
    pub struct Foo<A: Data> {
        pub a: A,
    }

    #[data]
    pub struct Bar<A: Data, B: Data> {
        pub a: A,
        pub b: B,
    }

    #[test]
    fn test() {
        assert_eq!(ok2(4711), 4711);
        assert_eq!(ok4(), 4711);
    }
}
