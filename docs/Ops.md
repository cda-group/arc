<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# Dialect 'arc' definition

[TOC]

## Operation definition

### arc.acos (arc::Acos)
arcus cosine of the specified value

#### Description:


The `sin` operation computes the arcus cosine of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.asin (arc::Asin)
arcus sine of the specified value

#### Description:


The `sin` operation computes the arcus sine of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.atan (arc::Atan)
arcus tangent of the specified value

#### Description:


The `tan` operation computes the arcus tangent of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.block.result (arc::BlockResult)
specifies the value of a block

#### Description:


"arc.block.result" is a special terminator operation for the block inside
"arc.if". It terminates the region. It should have the same type as the
parent if.

```mlir
  "arc.block.result"(%b) : (f32) -> f64
```

#### Operands:

1. `result`: any type

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.cosh (arc::Cosh)
hyperbolic cosine of the specified value

#### Description:


The `cosh` operation computes the hyperbolic cosine of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.erf (arc::Erf)
error function of the specified value

#### Description:


The `erf' operation computes the Gauss error function of a given
value. It takes one operand and returns one result of the same
type. This type may be a float scalar type, a vector whose element
type is float, or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.if (arc::If)
if-then-else operation

#### Description:


The "arc.if" operation represents an if-then-else construct for
conditionally executing two regions of code. The operand to an if operation
is a boolean value. The operation produces a single result. For example:

```mlir
"arc.if"(%a) ( {
  "arc.block.result"(%b) : (f64) -> f64
},  {
  "arc.block.result"(%c) : (f64) -> f64
}) : (i1) -> f64
```

#### Operands:

1. `condition`: 1-bit integer

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.index_tuple (arc::IndexTuple)
index into a tuple using a constant value

#### Description:


A pure operation which reads a value from a tuple

#### Operands:

1. `values`: tuple

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
| `index` | `IntegerAttr` | non-negative 64-bit integer attribute attribute |

#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.keep (arc::Keep)
Operation consuming anything and having side effects.

#### Description:


An operation used for debugging and testing. Can be used
to prevent optimization passes from removing dummy
results.

#### Operands:

1. `values`: any type

#### Attributes:


#### Results:


### arc.make_tuple (arc::MakeTuple)
create a tuple from a sequence of values

#### Description:


A pure operation which turns its arguments into a tuple

#### Operands:

1. `values`: any type

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: tuple

### arc.make_vector (arc::MakeVector)
create a vector from a sequence of values

#### Description:


A pure operation which turns its arguments into a vector

#### Operands:

1. `values`: any type

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: 1D tensor of any type values

### arc.sin (arc::Sin)
sine of the specified value

#### Description:


The `sin` operation computes the sine of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.sinh (arc::Sinh)
hyperbolic sine of the specified value

#### Description:


The `sinh` operation computes the hyperbolic sine of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

### arc.tan (arc::Tan)
tangent of the specified value

#### Description:


The `tan` operation computes the tangent of a given
value.It takes one operand and returns one result of the same
type.This type may be a float scalar type,
a vector whose element type is float,
or a tensor of floats.It has no standard attributes.

#### Operands:

1. `operand`: floating-point-like

#### Attributes:


#### Results:

1. &laquo;unnamed&raquo;: any type

