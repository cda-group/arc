// use rustpython_vm as rp;
// use rp::compile;
// use rp::VirtualMachine;
// use rp::eval;

#[test]
fn test() {
    //     let mut vm = VirtualMachine::new();
    //     let builtin = vm.get_builtin_scope();
    //     let scope = vm.new_scope(Some(builtin));
    //     vm.run_code_obj(code, scope);
}
